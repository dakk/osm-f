module StreetsGen.Test
    
open osmf.Utility
open NUnit.Framework


module StreetsGraph =
    let [<Test>] ``Export to OSM`` () =
        try
            
            assert (false)
        with
            | e -> assert (true)
   

                
